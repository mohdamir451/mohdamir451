@model UserUpsertRequest
@{
    ViewData["Title"] = "Edit User";
}

<section class="card form-card user-editor premium" id="users">
    <div class="section-header">
        <div>
            <h2>Edit User</h2>
            <p class="muted">Update account details, role and current activation state.</p>
        </div>
        <a asp-action="Index" class="btn btn-outline">Back to List</a>
    </div>
    <partial name="_UserForm" model="Model" />
</section>
